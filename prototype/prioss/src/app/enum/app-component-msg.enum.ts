
/**
 * An enum containing all the possible messages that a component can send to the AppComponent
 * 
 * @author: Simon (scg@mail.upb.de)
 */
export enum AppComponentMsg
{
    none = "none",
    backToDashboard = "backToDashboard"
}