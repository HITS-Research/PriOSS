import { PascalSpacingPipe } from './pascal-spacing.pipe';

describe('PascalSpacingPipe', () => {
  it('create an instance', () => {
    const pipe = new PascalSpacingPipe();
    expect(pipe).toBeTruthy();
  });
});
