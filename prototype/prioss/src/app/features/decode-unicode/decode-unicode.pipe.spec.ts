import { DecodeUnicodePipe } from './decode-unicode.pipe';

describe('DecodeUnicodePipe', () => {
  it('create an instance', () => {
    const pipe = new DecodeUnicodePipe();
    expect(pipe).toBeTruthy();
  });
});
